<Window x:Class="ZeroInstall.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ZeroInstall.App.ViewModels"
        xmlns:views="clr-namespace:ZeroInstall.App.Views"
        xmlns:converters="clr-namespace:ZeroInstall.App.Converters"
        mc:Ignorable="d"
        Title="ZeroInstall Migrator"
        Height="650" Width="900"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource SurfaceLightGrayBrush}">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />

        <!-- ViewModel to View data templates -->
        <DataTemplate DataType="{x:Type vm:WelcomeViewModel}">
            <views:WelcomeView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:DiscoveryViewModel}">
            <views:DiscoveryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CaptureConfigViewModel}">
            <views:CaptureConfigView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RestoreConfigViewModel}">
            <views:RestoreConfigView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MigrationProgressViewModel}">
            <views:MigrationProgressView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:JobSummaryViewModel}">
            <views:JobSummaryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProfileListViewModel}">
            <views:ProfileListView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ProfileEditorViewModel}">
            <views:ProfileEditorView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:JobHistoryViewModel}">
            <views:JobHistoryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header (gradient bar) -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryGradientBrush}" Padding="20,14">
            <Grid>
                <TextBlock Text="ZeroInstall Migrator"
                           Foreground="White"
                           FontSize="20"
                           FontWeight="Bold"
                           VerticalAlignment="Center" />
                <Button Command="{Binding OpenSettingsCommand}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Cursor="Hand"
                        Padding="8,4"
                        ToolTip="Settings">
                    <TextBlock Text="&#x2699;"
                               Foreground="White"
                               FontSize="20" />
                </Button>
            </Grid>
        </Border>

        <!-- Step Indicator -->
        <Border Grid.Row="1"
                Background="{StaticResource BackgroundWhiteBrush}"
                BorderBrush="{StaticResource BorderGrayBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,10">
            <ItemsControl ItemsSource="{Binding StepNames}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,0,8,0">
                            <TextBlock Text="{Binding}"
                                       VerticalAlignment="Center"
                                       FontSize="13"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       Margin="0,0,16,0" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- Content Area -->
        <ContentControl Grid.Row="2"
                        Content="{Binding CurrentViewModel}"
                        Margin="0" />

        <!-- Footer / Nav Bar -->
        <Border Grid.Row="3"
                Background="{StaticResource BackgroundWhiteBrush}"
                BorderBrush="{StaticResource BorderGrayBrush}"
                BorderThickness="0,1,0,0"
                Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Content="Back"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding GoBackCommand}"
                        Visibility="{Binding CanGoBack, Converter={StaticResource BoolToVisibility}}"
                        Padding="16,8" />

                <TextBlock Grid.Column="1"
                           Text="{Binding StatusText}"
                           Style="{StaticResource BodyText}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center" />

                <Button Grid.Column="2"
                        Content="Forward"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding GoForwardCommand}"
                        Visibility="{Binding CanGoForward, Converter={StaticResource BoolToVisibility}}"
                        Padding="16,8" />
            </Grid>
        </Border>
    </Grid>
</Window>
